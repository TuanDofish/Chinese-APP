# Bộ biểu đồ standalone cho PlantUML Web Server

Các file trong thư mục này không dùng `!include`.

Bạn chỉ cần:
1. Mở một file `.puml`
2. Copy toàn bộ nội dung
3. Dán trực tiếp vào `plantuml.com/plantuml`

Nếu dùng PlantUML Web Server, hãy ưu tiên bộ này thay vì thư mục `thesis-style`.
